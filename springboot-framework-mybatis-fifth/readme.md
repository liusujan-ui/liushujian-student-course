### 知识点 mybatis配置版
> 1.配置mybatis-config.xml、studentMapper.xml两个文件
> 2.Dao层中的studentMapper接口要和xml方法对应起来
> 3.SqlSessionFactoryUtil类加载mybatis-config.xml里面的配置
> 4.如果@test运行不了了，要将springboot先跑一下，然后删除target里面的编译文件即可
> 